# Requirements for M3 Max (Apple Silicon)
# Install with: pip install -r requirements-m3.txt
#
# Notes:
# - torch installs with MPS (Metal) support by default on macOS ARM
# - torch-geometric does NOT support MPS well — use CPU for PyG ops
# - The calibration script uses CPU by default (reproducibility baseline)
# - If you want to try MPS for TinyLlama inference: device="mps" in FrozenLLM
#   but verify results match CPU before using for calibration

# Core
numpy>=1.24,<3.0
scipy>=1.10
networkx>=3.0
scikit-learn>=1.3

# Torch (Apple Silicon — MPS backend included by default)
torch>=2.3,<3.0
torchvision
torchaudio

# PyTorch Geometric (CPU only on Apple Silicon)
torch-geometric>=2.5

# Transformers + LoRA
transformers>=4.40
peft>=0.10

# RL
gymnasium>=0.29.1,<1.3.0
stable-baselines3>=2.7,<3.0
sb3-contrib>=2.7,<3.0

# Test
pytest>=7.0
